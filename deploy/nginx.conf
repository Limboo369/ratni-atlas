server {
    listen 80;
    root /usr/share/nginx/html;
    gzip on;
    gzip_types application/json application/javascript text/css;
    # every game has its own link (/game-<code>): same page, the game reads the code from the address
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }
    # map data (the world: data/svijet/map.json, era_<id>.json): a missing file is a 404, not the game page,
    # so the game can tell which maps and eras exist
    location /data/ {
        try_files $uri =404;
        add_header Cache-Control "no-cache";
    }
    # online relay (deploy/game); resolver so a recreated game container is found again
    location /ws {
        access_log off;
        resolver 127.0.0.11 valid=2s;
        set $game http://game:8080;
        proxy_pass $game;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Origin $http_origin;
        proxy_set_header X-Real-IP $http_x_real_ip;
        proxy_connect_timeout 3s;
        proxy_read_timeout 1h;
    }
}
